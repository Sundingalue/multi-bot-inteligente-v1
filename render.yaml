services:
  - type: web
    name: multi-bot-inteligente-v1
    env: python
    plan: free
    branch: main
    buildCommand: ""
    startCommand: "gunicorn -k eventlet -w 1 main:app"
    envVars:
      - fromDotEnv: true
